# 拍摄策划 H5（命名待定）

一个面向**被拍的人**（情侣、个人、家庭等拍照主体，而非摄影师）的 H5 工具：输入拍摄主题和少量前置条件，AI 生成一套或多套拍摄策划，目标是提高出片率。

## Language

**被拍者 (Subject)**:
产品的目标用户：镜头前的人。他们在乎"拍出来好不好看"，不在乎摄影术语。
_Avoid_: 摄影师、用户（泛指时）

**前置条件 (Brief)**:
被拍者提供的输入：一段自由描述文本，可附最多 4 张参考图（模特图/场景图）。主题、地点、时间、人物等维度由 AI 从文本与图片解析，不做结构化表单。
_Avoid_: 参数、表单

**策划 (Shoot Plan)**:
一次生成的完整交付物：一组按场景组织的画面描述（姿势、构图、光线、表情引导），供被拍者带到现场照着拍。
_Avoid_: 方案、脚本、prompt

**画面 (Shot)**:
策划的最小单位：一个可模仿的具体镜头描述，对应成片中的一张照片。
_Avoid_: 机位、镜头（易与器材混淆）

**场景 (Scene)**:
一组在同一地点/时段拍摄的画面的分组。
_Avoid_: 章节、部分

**方案讨论 (Discussion)**:
前置条件与生成策划之间的对话环节。被拍者先和 AI 聊清风格调性、服装、道具、动作，聊完手动归纳成「拍摄约定」，确认后才进入生成。目的：把"风格跑偏"挡在生成之前，而不是事后返工。
_Avoid_: 聊天、客服、问答

**拍摄约定 (Consensus)**:
讨论环节的产出一份结构化约定（风格调性 / 服装 / 道具 / 动作与情绪 / 其他约束），会被注入生成用的 prompt，锁死风格。生成出的 3 套方案是**同一约定的不同场景变体**，不再各自换风格。
_Avoid_: 共识、结论、需求单

**流式回复 (Streaming Reply)**:
讨论环节 AI 回复的呈现方式——逐字往外冒，而不是等整段回来再一次性显示。技术上走 SSE；厂商不支持时自动退化成一次性显示，用户无感。
_Avoid_: 打字机效果（那是表现形式，不是这件事本身）、loading

**场景变体 (Scene Variant)**:
风格方向被约定锁定后，一次生成里 3 套策划之间的关系：同一调性、不同拍摄场景（如"咖啡馆窗边""街角斑马线""楼顶天台"），供被拍者按场地挑一套。
_Avoid_: 风格方向（那是约定出现前的老概念，已收敛）

**插画库 (Pose Library)**:
预先批量绘制、按姿势标签组织的简笔画姿势图集合。画面未生成参考片时作为姿势示意兜底。
_Avoid_: 参考图、素材库

**参考图 (Reference Image)**:
被拍者随前置条件上传的风格参考照片，分两类：模特图（人物外形气质）和场景图（真实环境）。随请求发给用户所选模型（需支持看图），用于"图生图"——机位和动作贴着图片生成，而非凭空想象。
_Avoid_: 灵感图、素材

**参考片 (Reference Shot)**:
AI 为单个画面按需生成的写实参考照片，可选功能（默认豆包 Seedream，用户只填 Key）。生成时用户的参考图以 base64 直传（图生图），参考片保持同一人物、同一场景；可单张或批量生成，未生成时以插画库姿势示意代替。
_Avoid_: 参考图（易与上传的参考图混淆）、生图
