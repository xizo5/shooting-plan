# 依赖与构建产物
node_modules
dist
dist-ssr

# 包管理器：本项目统一用 npm，禁止混入其他锁文件
# （2026-09-16：曾被工具自动改造成 pnpm 项目，两次安装的依赖树不一致，
#   导致 IDE 依赖检查报 ERR_PNPM_IGNORED_BUILDS 且极难排查）
pnpm-lock.yaml
pnpm-workspace.yaml
yarn.lock

# 环境与本地覆盖（可能含真实 API Key，绝不入库）
.env
.env.*
!.env.example
*.local

# 编辑器与系统
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# 日志
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 本地工具缓存（非项目数据）
.workbuddy

# 临时调试产物（截图、注入诊断的页面副本、编译中间件）
tmp

# 截图演示用的参考片（base64，210KB）：由 tools 临时生成，不入库
# 重新生成见 AGENTS.md「官网截图怎么来的」
public/shot-refs.js
